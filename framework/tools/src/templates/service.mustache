{{{ESM_EXPORT}}}class {{{CLASS_NAME}}} extends Service {
    constructor(primaryKey) {
        super(primaryKey);
    }
{{{METHODS}}}
}
client.registerService('{{{CLASS_NAME}}}', {{{CLASS_NAME}}}, WORKER_KEY, {{{CLASS_ID}}});